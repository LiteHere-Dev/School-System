{
  "name": "school-management-server",
  "version": "162.0.0",
  "description": "School Management System — V162 makes the AI model auto-switcher search across every parameter size actually pulled for each installed model family (e.g. qwen3:8b vs qwen3:14b), instead of assuming an untagged/'latest' tag exists, and verifies capability-routing/vision-attachment checks against Ollama's real live model list rather than a static assumption.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.8.0",
    "xlsx": "^0.18.5",
    "adm-zip": "^0.5.16",
    "tar-stream": "^3.1.7"
  }
}
